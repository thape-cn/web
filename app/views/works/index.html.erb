<%= render partial: 'shared/thape_nav'  -%>

<div class="w-full mx-auto px-2 md:px-6 lg:px-8 xl:px-10 xxl:px-12 xxxl:px-16 pt-0 md:pt-0 lg:pt-0 xl:pt-0 xxl:pt-0 xxxl:pt-0">
  <%- content_for :header_right do %>
    <div class="block w-full mt-2 sm:w-full lg:w-auto sm:mt-4 lg:mt-0">
      <%= render partial: 'search_form' %>
    </div>
  <% end %>
  <%= render partial: 'shared/header', locals: { c: '所有项目', e: 'PROJECTS',
    header_path: works_path, header_class: 'border-gray-200 border-solid border-b-0 lg:border-b', show_header_right: true } %>
</div>
<div class="w-full mx-auto sm:pb-4 md:pb-8 px-1 md:px-3 lg:px-4 xl:px-5 xxl:px-6 xxxl:px-8">

  <%= render partial: 'works_type', locals: { work_type_page: @work_type_page } %>

</div>

<%= render partial: 'shared/footer' %>
