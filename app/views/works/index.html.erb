<%= render partial: 'shared/thape_nav'  -%>

<div class="w-full mx-auto p-2 md:p-6 lg:p-8 xl:p-10 xxl:p-12 xxxl:p-16 pt-0 md:pt-0 lg:pt-0 xl:pt-0 xxl:pt-0 xxxl:pt-0">
  <%- content_for :header_right do %>
    <div class="block w-full mt-2 sm:w-full lg:w-auto sm:mt-4 lg:mt-0">
      <%= render partial: 'search_form' %>
    </div>
  <% end %>
  <%= render partial: 'shared/header', locals: { c: '所有项目', e: 'PROJECTS',
    header_path: works_path, header_class: 'border-gray-200 border-solid border-b', show_header_right: true } %>

  <%= render partial: 'works_type' %>

</div>

<%= render partial: 'shared/footer' %>
