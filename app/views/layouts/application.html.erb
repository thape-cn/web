<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Web</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <script type="module" src="/alpine/2-4-1.js"></script>
    <script nomodule src="/alpine/2-4-1-ie11.js" defer></script>
    <%= javascript_pack_tag 'application' %>
    <% if browser.ie? %>
      <style>#thape-top-header { top: 56px; }</style>
    <% end %>
  </head>

  <body class="bg-white overflow-x-hidden min-h-screen flex flex-wrap">
    <% flash.each do |type, message| %>
      <% if type == "alert" %>
        <div class="bg-red-500 fixed top-0 left-0 w-full z-50">
          <div class="container px-2 mx-auto py-4 text-white text-center font-medium font-sans"><%= message %></div>
        </div>
      <% end %>
      <% if type == "notice" %>
        <div class="bg-green-500 fixed top-0 left-0 w-full z-50">
          <div class="container px-2 mx-auto py-4 text-white text-center font-medium font-sans"><%= message %></div>
        </div>
      <% end %>
    <% end %>

    <%= yield %>
  </body>
</html>
