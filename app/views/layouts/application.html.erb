<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="renderer" content="webkit">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Web</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <script type="module" src="/alpine/2-4-1.js"></script>
    <script nomodule src="/alpine/2-4-1-ie11.js" defer></script>
    <%= javascript_pack_tag 'application' %>
    <% if browser.ie? %>
      <style>
        #thape-footer-logo { height: 22px; }
      </style>
    <% end %>
  </head>

  <body class="overflow-x-hidden">
    <% flash.each do |type, message| %>
      <% if type == "alert" %>
        <div class="bg-red-600 w-full fixed z-50">
          <div class="container px-2 mx-auto py-4 text-white text-center font-medium font-sans"><%= message %></div>
        </div>
        <div class="bg-red-600 w-full">
          <div class="container px-2 mx-auto py-4 text-white text-center font-medium font-sans"><%= message %></div>
        </div>
      <% end %>
      <% if type == "notice" %>
        <div class="bg-green-600 w-full fixed z-50">
          <div class="container px-2 mx-auto py-4 text-white text-center font-medium font-sans"><%= message %></div>
        </div>
        <div class="bg-green-600 w-full">
          <div class="container px-2 mx-auto py-4 text-white text-center font-medium font-sans"><%= message %></div>
        </div>
      <% end %>
    <% end %>
    <div class="relative bg-white flex flex-wrap main" id="main">
      <%= yield %>
    </div>
    <script>
      (function(){
        function setMainHeight() {
          const mainElem = document.querySelector('#main');
          mainElem.style.height = 'calc(100vh - ' + mainElem.offsetTop + 'px)'
        }
        setMainHeight();
        window.addEventListener('resize', setMainHeight);
      })()
    </script>
  </body>
</html>
